[package]
name = "agentgateway"
version = { workspace = true }
license = { workspace = true }
edition = { workspace = true }
publish = { workspace = true }

[lib]
path = "src/lib.rs"

[features]
default = ["jemalloc", "mimalloc", "crypto-aws-lc"]
jemalloc = ["dep:tikv-jemallocator", "dep:jemalloc_pprof"]
mimalloc = ["pprof-alloc/allocator-mimalloc", "dep:mimalloc"]
# Crypto provider selection. `crypto-aws-lc` (the default) backs all crypto with
# aws-lc-rs. `fips` is an operating-mode modifier for `crypto-aws-lc` or
# `crypto-boring`. With `crypto-aws-lc` it builds against AWS-LC-FIPS
# (aws-lc-fips-sys); the module is determined by the aws-lc-fips-sys version
# resolved in Cargo.lock. With `crypto-boring` it builds the FIPS module from the
# BoringSSL source vendored by boring-sys.
# Note the linked module version is not necessarily CMVP-validated; check the
# certificate for the exact module version before making a compliance claim.
# `crypto-symcrypt` routes TLS/AEAD/hash/RNG through SymCrypt and falls back to
# aws-lc-rs for JWT and certificate issuance (no SymCrypt hook there).
# `crypto-boring` links no aws-lc-rs and has no Postgres TLS. Its TLS runs on
# BoringCrypto, but boring-rustls-provider does not use the module's approved
# TLS record, TLS 1.3 key derivation and ECDH services.
crypto-aws-lc = ["dep:aws-lc-rs", "rustls/aws_lc_rs", "tokio-rustls/aws_lc_rs", "jsonwebtoken/aws_lc_rs", "_bundled-crypto-deps"]
# `rustls/fips` comes through aws-smithy-http-client, which only
# `_bundled-crypto-deps` enables.
# Enabling it directly would pull aws-lc-rs into `crypto-boring`.
fips = ["aws-lc-rs?/fips", "aws-smithy-http-client?/rustls-aws-lc-fips", "boring?/fips", "boring-rustls-provider?/fips"]
crypto-symcrypt = ["dep:symcrypt", "dep:rustls-symcrypt", "dep:aws-lc-rs", "jsonwebtoken/aws_lc_rs", "_bundled-crypto-deps"]
crypto-boring = ["dep:boring", "dep:boring-sys", "dep:boring-rustls-provider", "dep:foreign-types", "dep:aws-smithy-runtime-api"]
# These dependency features bundle aws-lc-rs or ring. Only `crypto-aws-lc` and
# `crypto-symcrypt` enable them.
_bundled-crypto-deps = [
    "aws-config/default-https-client",
    "dep:aws-smithy-http-client",
    "google-cloud-auth/default-rustls-provider",
    "dep:rcgen",
    "rcgen/aws_lc_rs",
    "rcgen/x509-parser",
    "reqwest/rustls",
    "sqlx/tls-rustls",
    "wiremock/tls",
]
ui = []
schema = ["schemars", "agent-core/schema", "agent-llm/schema"]
internal_benches = ["divan"]
assert_size_runtime = ["agent-core/assert_size_runtime"]

[dependencies]
agent-core.workspace = true
agent-celx.workspace = true
agent-hbone.workspace = true
agent-http.workspace = true
agent-llm.workspace = true
agent-xds.workspace = true
agent-pool.workspace = true
anyhow.workspace = true
arc-swap.workspace = true
async-compression.workspace = true
async-openai.workspace = true
async-trait.workspace = true
atomic_float.workspace = true
aws-config.workspace = true
aws-credential-types.workspace = true
aws-sigv4.workspace = true
aws-types.workspace = true
aws-lc-rs = { workspace = true, optional = true }
symcrypt = { workspace = true, optional = true }
rustls-symcrypt = { workspace = true, optional = true }
boring = { workspace = true, optional = true }
boring-sys = { workspace = true, optional = true }
boring-rustls-provider = { workspace = true, optional = true }
foreign-types = { workspace = true, optional = true }
# No code uses this crate. The `fips` feature enables `rustls/fips` through it.
aws-smithy-http-client = { workspace = true, optional = true }
aws-smithy-runtime-api = { workspace = true, optional = true }
aws-smithy-eventstream.workspace = true
aws-smithy-types.workspace = true
axum-core.workspace = true
axum-extra.workspace = true
axum.workspace = true
azure_core.workspace = true
azure_identity.workspace = true
base64.workspace = true
bytes.workspace = true
cookie.workspace = true
cel.workspace = true
chrono.workspace = true
clocksource.workspace = true
core_affinity.workspace = true
crossbeam.workspace = true
dashmap.workspace = true
divan = { workspace = true, optional = true }
frozen-collections.workspace = true
fs-err = { workspace = true }
futures-core.workspace = true
futures-util.workspace = true
futures.workspace = true
google-cloud-auth.workspace = true
hashbrown.workspace = true
headers.workspace = true
headers-accept = "0.3.0"
heck.workspace = true
hex.workspace = true
hickory-resolver.workspace = true
htpasswd-verify-fork.workspace = true
http-body-util.workspace = true
http-body.workspace = true
http-serde.workspace = true
http.workspace = true
httpdate.workspace = true
hyper-util.workspace = true
hyper.workspace = true
include_dir.workspace = true
indexmap = { workspace = true }
ipnet.workspace = true
itertools.workspace = true
jsonwebtoken.workspace = true
lazy_static.workspace = true
mediatype = "0.21.0"
notify-debouncer-full.workspace = true
notify.workspace = true
num_cpus.workspace = true
once_cell.workspace = true
multer.workspace = true
openapiv3.workspace = true
opentelemetry-otlp.workspace = true
opentelemetry-proto.workspace = true
opentelemetry-http.workspace = true
opentelemetry.workspace = true
opentelemetry_sdk.workspace = true
parking_lot.workspace = true
pem.workspace = true
percent-encoding.workspace = true
phonenumber.workspace = true
pin-project-lite.workspace = true
ppp.workspace = true
prometheus-client.workspace = true
protos.workspace = true
prost-types.workspace = true
prost-wkt-types.workspace = true
prost.workspace = true
rand.workspace = true
quick_cache.workspace = true
libc.workspace = true
rcgen = { workspace = true, optional = true }
regex.workspace = true

rmcp = { version = "3.1.0", features = [
    "client",
    "server",
    "base64",
    "server-side-http",
    "transport-child-process",
] }
rust_decimal.workspace = true
rustls-native-certs.workspace = true
rustls-pki-types.workspace = true
rustls.workspace = true
schemars = { workspace = true, optional = true }
secrecy.workspace = true
serde.workspace = true
serde_json.workspace = true
serde_regex.workspace = true
serde_urlencoded.workspace = true
serde_with.workspace = true
serde_norway.workspace = true
yaml-serde-edit.workspace = true
serde-untagged.workspace = true
stacker.workspace = true
shellexpand.workspace = true
socket2.workspace = true
spiffe.workspace = true
sse-stream.workspace = true
subtle.workspace = true
sqlx.workspace = true
thiserror.workspace = true
tiktoken-rs.workspace = true
mimalloc = { workspace = true, optional = true }
tokio-rustls.workspace = true
tokio-stream.workspace = true
tokio-util.workspace = true
tokio_sse_codec.workspace = true
tonic.workspace = true
tower-http.workspace = true
tower-serve-static.workspace = true
tower.workspace = true
tracing-subscriber.workspace = true
tracing.workspace = true
twox-hash.workspace = true
typespec_client_core.workspace = true
url.workspace = true
uuid.workspace = true
value-bag.workspace = true
x509-parser.workspace = true
zstd.workspace = true
reqwest.workspace = true
websocket-sans-io.workspace = true
vector-map.workspace = true
flagset.workspace = true
wiremock.workspace = true

# Linux only dependencies
# pprof's "frame-pointer" unwinder only compiles on these arches; elsewhere
# (e.g. s390x) it must be left off, and pprof falls back to its default unwinder.
[target.'cfg(all(target_family = "unix", any(target_arch = "x86_64", target_arch = "aarch64", target_arch = "riscv64", target_arch = "loongarch64")))'.dependencies]
pprof = { workspace = true, features = ["frame-pointer"] }

[target.'cfg(all(target_family = "unix", not(any(target_arch = "x86_64", target_arch = "aarch64", target_arch = "riscv64", target_arch = "loongarch64"))))'.dependencies]
pprof.workspace = true

# Windows only dependencies
[target.'cfg(target_family = "windows")'.dependencies]
which.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
tokio = { workspace = true, features = ["taskdump"] }
tikv-jemallocator = { workspace = true, optional = true }
jemalloc_pprof = { workspace = true, optional = true }

[target.'cfg(all(target_os = "linux", not(all(target_env = "musl", target_arch = "aarch64"))))'.dependencies]
pprof-alloc = { workspace = true, features = ["allocator-jemalloc"] }

[target.'cfg(all(target_os = "linux", target_env = "musl", target_arch = "aarch64"))'.dependencies]
pprof-alloc.workspace = true

[target.'cfg(not(target_os = "linux"))'.dependencies]
pprof-alloc.workspace = true
tokio = { workspace = true }

[dev-dependencies]
rcgen = { workspace = true, features = ["aws_lc_rs", "x509-parser"] }
# Enable the SPIFFE Workload API bindings for tests only (fake agent for SPIFFE dataplane tests).
protos = { workspace = true, features = ["spiffe-test-server"] }
assert_matches.workspace = true
divan.workspace = true
h2.workspace = true
insta.workspace = true
rstest.workspace = true
tempfile.workspace = true
tokio = { workspace = true, features = ["test-util"] }
which.workspace = true
rmcp = { version = "3.1.0", features = [
    "client",
    "server",
    "base64",
    "transport-child-process",
    "transport-streamable-http-client-reqwest",
    "transport-streamable-http-client",
    "transport-streamable-http-server",
    "reqwest",
    "auth",
] }
# We need SSE client for testing but they removed it for some reason :-(
# https://github.com/modelcontextprotocol/rust-sdk/pull/562
legacy-rmcp = { package = "rmcp", version = "0.10.0", features = ["transport-sse-server",
    "transport-sse-client",
    "transport-sse-client-reqwest",
    "client",
    "reqwest"
] }
reqwest.workspace = true
# TODO(https://github.com/modelcontextprotocol/rust-sdk/pull/667)
legacyreqwest = { package = "reqwest", version = "0.12", default-features = false, features = [
    "http2",
    "charset",
    "macos-system-configuration",
    "rustls-tls",
] }

[lints.clippy]
# This rule makes code more confusing
assigning_clones = "allow"
# This doesn't understand `strng` which we use everywhere
borrow_interior_mutable_const = "allow"
declare_interior_mutable_const = "allow"

[lints.rust]
linker_messages = "allow"
# This rule makes code more confusing
mismatched_lifetime_syntaxes = "allow"
# ossl350 is set by the openssl-sys crate when OpenSSL >= 3.5.0 is detected.
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(ossl350)'] }

[[bench]]
name = "bench_tests"
harness = false
